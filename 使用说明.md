# TraeSign 使用说明

TraeSign 是一个 Trae（TraeWork / Trae Solo / Trae CN）**每日自动签到托盘助手**：常驻系统托盘，自动完成每日签到领取积分，并在日历中记录每一天的签到结果。

> 程序本体**仅 `TraeSign.exe` 一个文件（约 40KB）**，登录态读取、解密、签到接口、托盘、日历等全部功能均已内置，**无需任何其它文件或运行环境**。

## 一、快速开始

1. 将 `TraeSign.exe`（约 40KB）放到任意目录 —— **单独运行这一个文件即可**，无需安装、无需 Node 等任何运行环境
2. 双击运行 → 程序缩到系统托盘（任务栏右下角）
3. 托盘图标变绿勾 = 今日已签到成功；图标悬停可查看签到账号与积分
4. 双击托盘图标 → 打开主窗口查看账号/积分/签到日历

> 首次运行若被 Windows Defender 拦截，点"仍要运行"（程序为本地自编译，无数字签名）。

## 二、自动签到

- 每天 **00:05** 自动签到（仅当前选中账号）
- 失败后自动重试（间隔 5 / 15 / 30 / 60 / 120 分钟递增），当天 23:00 后放弃
- 程序**保持运行**才生效（开机自启方法见下文"常见问题"）
- 也可随时点击主窗口的 **"立即签到（手动）"** 按钮

## 三、主窗口说明

| 区域 | 内容 |
|---|---|
| 标题行 | 左侧 TraeSign，右侧今日状态徽标（已签到/未签到/账号异常） |
| 账号区 | 下拉框切换账号；**"刷新账号状态"** 按钮（在 Trae 客户端重新登录后点此刷新） |
| 今日签到区 | 今日签到状态 + 积分（基础+额外） |
| 自动签到区 | "每日 00:05 自动签到（当前选中账号）"提示 + 下次重试时间 + 手动签到按钮 |
| 签到日历 | 当月日历：**绿色 = 签到成功**、今天红色描边、点顶部 ◀ ▶ 翻月 |

**托盘图标颜色**：灰 = 未签到/查询中；绿勾 = 已签到；红 = 失败/账号异常/被占用。

**气泡通知**：签到成功 / 失败原因 / "今天已签到，无需重复签到"（重复点击手动签到时）。

## 四、多账号支持

- 自动读取本机 **TRAE SOLO CN / Trae CN / TraeWork** 三个软件的登录态
- 主窗口下拉框可切换用哪个账号签到，切换后自动刷新该账号状态
- 各账号的签到历史独立记录，日历按当前选中账号显示

> **重要限制**：同机的多个 Trae 软件共享设备 ID，服务端规定**每天只有一个账号能签到成功**。若当天另一账号已签到，本账号会提示"本机今日已被另一账号签到"（9095），属正常现象，次日可再签到。

## 五、数据与配置

- 签到历史：`%APPDATA%\TraeCheckin\history.json`（删除即清空历史与选中账号）
- 自检日志：`%APPDATA%\TraeCheckin\selfcheck.log`
- 程序**只读** Trae 的登录态文件（`%APPDATA%\...\User\globalStorage\storage.json`），不修改、不泄露登录凭证

## 六、常见问题

**1. 提示"未找到登录态"？**
先在对应的 Trae 客户端（TRAE SOLO CN / Trae CN）登录一次，再打开 TraeSign 或点"刷新账号状态"。

**2. 提示"账号状态异常（token 可能已过期）"？**
Token 已过期。打开对应 Trae 客户端重新登录，然后点主窗口"刷新账号状态"。

**3. 提示"本机今日已被另一账号签到"？**
今天另一个账号已签到，服务端限制同机每天仅一个账号，明日再签。

**4. 想开机自动运行？**
把 `TraeSign.exe` 的快捷方式放入：`Win+R` 输入 `shell:startup` 打开的文件夹即可。

**5. 杀毒软件误报？**
程序由本机 csc 编译、无数字签名，属误报。添加信任后即可。

**6. 怎么卸载？**
直接删除 `TraeSign.exe`；如需清理数据，一并删除 `%APPDATA%\TraeCheckin\` 文件夹。

## 七、开发者（重新编译）

```
compile.bat
```
- 依赖 Windows 自带 .NET Framework 4.8（`csc.exe`）
- 输出 `TraeSign.exe`（单文件，无外部依赖）
- 源码：`TraeCheckinApp.cs`（主程序）、`CalendarControl.cs`（自绘日历）

## 八、目录文件说明

| 文件 | 用途 |
|---|---|
| **TraeSign.exe** | 程序本体，**唯一必需文件** |
| TraeCheckinApp.cs / CalendarControl.cs | C# 源码（编译用） |
| compile.bat | 重新编译脚本 |
| trae-checkin.js | 早期 Node 版签到脚本（**逻辑已完全内置 exe**，仅作参考，可保留可删除） |
| 使用说明.md | 本文档 |